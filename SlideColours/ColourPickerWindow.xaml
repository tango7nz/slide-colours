<Window x:Class="SlideColours.ColourPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose a colour"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ResizeMode="NoResize" SizeToContent="WidthAndHeight"
        ShowInTaskbar="False" WindowStartupLocation="Manual">

    <Window.Resources>
        <!-- Dark text field -->
        <Style x:Key="HexField" TargetType="TextBox">
            <Setter Property="Background" Value="#FF2C2C36"/>
            <Setter Property="Foreground" Value="#FFF0F0F4"/>
            <Setter Property="CaretBrush" Value="#FFF0F0F4"/>
            <Setter Property="BorderBrush" Value="#40FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Pill-style push button -->
        <Style x:Key="PillButton" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="#FFF0F0F4"/>
            <Setter Property="Background" Value="#FF3A3A46"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource PillButton}">
            <Setter Property="Background" Value="#FF34C759"/>
            <Setter Property="Foreground" Value="#FF10281A"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="16" Background="#F0202028" Padding="16"
            BorderBrush="#40FFFFFF" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="3" Opacity="0.55"/>
        </Border.Effect>

        <StackPanel Width="286">

            <!-- Header: title (drag handle) + live preview -->
            <Grid Margin="2,0,2,12" x:Name="Header" Background="Transparent"
                  MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <TextBlock Text="Choose a colour" Foreground="#FFEDEDF2"
                           FontFamily="Segoe UI" FontSize="14" FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
                <Border Width="30" Height="30" CornerRadius="15" HorizontalAlignment="Right"
                        BorderBrush="#60FFFFFF" BorderThickness="1.5">
                    <Border.Background>
                        <SolidColorBrush x:Name="PreviewBrush" Color="Black"/>
                    </Border.Background>
                </Border>
            </Grid>

            <StackPanel Orientation="Horizontal">
                <!-- Saturation / Value square -->
                <Canvas x:Name="SvCanvas" Width="230" Height="170" ClipToBounds="True"
                        Cursor="Cross"
                        MouseLeftButtonDown="SvCanvas_MouseDown"
                        MouseMove="SvCanvas_MouseMove"
                        MouseLeftButtonUp="SvCanvas_MouseUp">
                    <Rectangle x:Name="SvBase" Width="230" Height="170" RadiusX="6" RadiusY="6"/>
                    <Rectangle Width="230" Height="170" RadiusX="6" RadiusY="6">
                        <Rectangle.Fill>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#FFFFFFFF" Offset="0"/>
                                <GradientStop Color="#00FFFFFF" Offset="1"/>
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>
                    <Rectangle Width="230" Height="170" RadiusX="6" RadiusY="6">
                        <Rectangle.Fill>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#00000000" Offset="0"/>
                                <GradientStop Color="#FF000000" Offset="1"/>
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>
                    <Ellipse x:Name="SvThumb" Width="14" Height="14" IsHitTestVisible="False"
                             Stroke="White" StrokeThickness="2" Fill="#40000000"/>
                </Canvas>

                <!-- Hue bar -->
                <Canvas x:Name="HueCanvas" Width="20" Height="170" Margin="12,0,0,0"
                        Cursor="SizeNS"
                        MouseLeftButtonDown="HueCanvas_MouseDown"
                        MouseMove="HueCanvas_MouseMove"
                        MouseLeftButtonUp="HueCanvas_MouseUp">
                    <Rectangle Width="20" Height="170" RadiusX="6" RadiusY="6">
                        <Rectangle.Fill>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#FFFF0000" Offset="0.000"/>
                                <GradientStop Color="#FFFFFF00" Offset="0.167"/>
                                <GradientStop Color="#FF00FF00" Offset="0.333"/>
                                <GradientStop Color="#FF00FFFF" Offset="0.500"/>
                                <GradientStop Color="#FF0000FF" Offset="0.667"/>
                                <GradientStop Color="#FFFF00FF" Offset="0.833"/>
                                <GradientStop Color="#FFFF0000" Offset="1.000"/>
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>
                    <Border x:Name="HueThumb" Width="24" Height="6" Canvas.Left="-2"
                            IsHitTestVisible="False" CornerRadius="3"
                            Background="Transparent" BorderBrush="White" BorderThickness="2"/>
                </Canvas>
            </StackPanel>

            <!-- Hex field + actions -->
            <Grid Margin="0,14,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBlock Text="#" Foreground="#FFB8B8C0" FontFamily="Consolas" FontSize="14"
                               VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <TextBox x:Name="HexBox" Style="{StaticResource HexField}" Width="86"
                             MaxLength="6" KeyDown="HexBox_KeyDown" LostFocus="HexBox_LostFocus"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Style="{StaticResource PillButton}"
                            Click="Cancel_Click" Margin="0,0,8,0"/>
                    <Button Content="OK" Style="{StaticResource PrimaryButton}" Click="Ok_Click"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Border>
</Window>
